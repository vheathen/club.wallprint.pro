<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Scribit: Unofficial</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
  </head>
  <body class="w-100 sans-serif">
    <header class="cf">
      <section class="container">
        <nav role="navigation" class="pa3 pa4-ns">
          <a class="link dim black b f1 f-headline-ns tc db mb3 mb4-ns" href="/" title="Home">Scribit Unofficial</a>
          <div class="tc pb3">
            <%= if Pow.Plug.current_user(@conn) do %>
              <span><%= link "Sign out", to: Routes.pow_session_path(@conn, :delete), method: :delete, class: "link dim gray f6 f5-ns dib mr3" %></span>
            <% else %>
              <span><%= link "Sign in", to: Routes.pow_session_path(@conn, :new), class: "link dim gray f6 f5-ns dib mr3" %></span>
            <% end %>
          </div>
        </nav>
      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= render @view_module, @view_template, assigns %>
    </main>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
